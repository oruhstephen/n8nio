FROM n8nio/runners:latest
USER root
WORKDIR /opt/runners/task-runner-python

# Add all the packages you need here, separated by spaces
RUN pip install --upgrade pip
RUN pip install --no-cache-dir websockets requests pandas numpy websocket-client finnhub-python polars \
    scipy matplotlib seaborn plotly streamlit \
    statsmodels xgboost


USER runner
